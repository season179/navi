{
  "name": "navi",
  "version": "0.0.1",
  "private": true,
  "description": "a local-first desktop app",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron ."
  },
  "devDependencies": {
    "electron": "^39.8.10",
    "typescript": "^6.0.3"
  }
}
