// Kun workspace-select empty hero chrome
// (../Kun/src/renderer/src/components/chat/message-timeline-empty.tsx).
// Visual only — used for production WorkspaceSelectEmptyHero and preview hooks.

/** English copy matching Kun's selectWorkspace locale string. */
export const WORKSPACE_SELECT_EMPTY_HERO_TITLE = 'Choose working directory'

/** English copy matching Kun's emptyHeroSubNoWorkspace locale string. */
export const WORKSPACE_SELECT_EMPTY_HERO_SUB =
  'Pick a local working directory first, then start your first thread.'

/** English copy matching Kun's selectWorkspace locale string (chip button). */
export const WORKSPACE_SELECT_EMPTY_HERO_BUTTON = 'Choose working directory'
