// Kun LlmDebugSettingsSection chrome
// (../Kun/src/renderer/src/components/settings-section-llm-debug.tsx).
// Visual only — used for production LlmDebugSettingsSection and preview hooks.

/** English copy matching Kun's sectionLlmDebug locale string. */
export const LLM_DEBUG_SETTINGS_TITLE = 'LLM request troubleshooting'

/** English copy matching Kun's llmDebugDesc locale string. */
export const LLM_DEBUG_SETTINGS_DESC =
  'Inspect the last 25 raw request bodies sent to the model (system prompt, messages, tools) and their raw output. Kept in memory only and cleared on restart.'

/** English copy matching Kun's llmDebugEmpty locale string. */
export const LLM_DEBUG_SETTINGS_EMPTY = 'No records yet. Send a message, then click Refresh.'

/** English copy matching Kun's llmDebugRequest locale string. */
export const LLM_DEBUG_SETTINGS_REQUEST = 'Request (HTTP body sent to the model)'

/** English copy matching Kun's llmDebugOutput locale string. */
export const LLM_DEBUG_SETTINGS_OUTPUT = 'Output'

/** English copy matching Kun's llmDebugReasoning locale string. */
export const LLM_DEBUG_SETTINGS_REASONING = 'Reasoning'

/** English copy matching Kun's llmDebugToolCalls locale string. */
export const LLM_DEBUG_SETTINGS_TOOL_CALLS = 'Tool calls'

/** English copy matching Kun's llmDebugUsage locale string. */
export const LLM_DEBUG_SETTINGS_USAGE = 'Usage'

/** English copy matching Kun's refresh locale string. */
export const LLM_DEBUG_SETTINGS_REFRESH_LABEL = 'Refresh'
